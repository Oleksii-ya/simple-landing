import { Typography } from "@mui/material"


const Title = ({ padding }) => {
  return (
    <Typography
      variant="h4"
      align="center"
      sx={{ padding }}
    >Waiting for their owners</Typography>
  )
}
export default Title